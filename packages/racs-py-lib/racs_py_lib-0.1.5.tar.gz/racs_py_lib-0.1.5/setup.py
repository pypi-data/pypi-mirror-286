from setuptools import setup, find_packages

setup(
    name="racs-py-lib",
    version="0.1.5",
    packages=find_packages(),
    install_requires=[
        "requests"
    ],
    author="miilkaa",
    
)
