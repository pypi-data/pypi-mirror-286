[project]
name = "app_injector"
version = "0.0.2"
dependencies = []
requires-python = ">= 3.10"

[project.scripts]
app-injector = "app_injector.main:main"

[tool.pyright]
venvPath = "."
venv = "venv"
