from setuptools import setup, find_packages

setup(
    name='sysRecurseSearch',
    version='0.1.2',  # Increment the version number here
    packages=find_packages(),
    install_requires=[
        # Add your package dependencies here
    ],
    entry_points={
        'console_scripts': [
            # Add console scripts here
        ],
    },
)