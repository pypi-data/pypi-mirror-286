[tool:pytest]
addopts = --doctest-modules --ignore=setup.py
testpaths = tests
norecursedirs = .* *.egg build cpp* dist docs/* tests/kitex tests/kitetools kite
minversion = 3.0
filterwarnings = ignore::ImportWarning

[egg_info]
tag_build = 
tag_date = 0

