[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ddi-transformations"
version = "0.0.38"
authors = [
  { name="highradius", email="highradius@highradius.com" },
]
description = "Transformations pypi package"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

