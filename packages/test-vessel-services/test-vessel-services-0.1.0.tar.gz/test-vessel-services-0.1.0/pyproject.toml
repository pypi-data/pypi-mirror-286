[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["app"]

[project] 
name = "test-vessel-services" 
version = "0.1.0" 
description = "A short description of your project"
requires-python = ">=3.6"