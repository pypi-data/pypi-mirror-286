[build-system]
requires = [
    "setuptools>=30.3.0",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 140
target-version = ['py310']
skip-string-normalization = true

[tool.isort]
profile = "black"
