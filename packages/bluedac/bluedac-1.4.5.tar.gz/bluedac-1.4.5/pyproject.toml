[tool.poetry]
name = "bluedac"
version = "1.4.5"
description = ""
authors = ["ChristianP01"]
readme = "README.md"

exclude = [
    "bluedac/LICENSE",
    "bluedac/README.md",
    "bluedac/requirements.txt",
    "bluedac/.git",
    "bluedac/.gitignore"
]

[tool.poetry.dependencies]
python = "^3.1"
aws-cdk-lib = "^2.144.0"
PyYAML = "^6.0.1"
moto = "^5.0.9"
pytest = "^8.2.2"
pytest-cov = "^5.0.0"
inquirer = "^3.2.5"

[tool.poetry.scripts]
dac = "bluedac.dac:launch_dac"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
