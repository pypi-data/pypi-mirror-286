[tool.poetry]
name = "lytils"
version = "0.1.33"
description = "Python utilities. Pytils was taken."
authors = ["GGLionCross"]
license = "Apache 2.0"
readme = "README.md"


[tool.poetry.dependencies]
python = "^3.10"
requests = {version = "^2.32.3", optional = true}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
