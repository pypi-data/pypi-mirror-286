from setuptools import setup, find_packages

setup(
    name='p_exe',
    version='0.1.1',
    packages=find_packages(),
    install_requires=[
        'maturin>=1.7.0'
    ],
    python_requires='>=3.7',
    long_description='Nuh uh'
)